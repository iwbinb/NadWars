[profile.default]
src = "src"
test = "test"
out = "out"
cache_path = "cache"
solc_version = "0.8.30"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
via_ir = true
bytecode_hash = "none"
network = "monad"
hardfork = "monad:MonadTen"
gas_limit = 30000000

[profile.default.fuzz]
runs = 256
seed = "0x4e616457617273"

[profile.ethereum]
network = "ethereum"
hardfork = "ethereum:Cancun"
